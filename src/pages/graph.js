import styles from "./graph.module.css";

const Graph = () => {
  return (
    <div className={styles.graph}>
      <img className={styles.graphChild} alt="" src="/vector-3.svg" />
    </div>
  );
};

export default Graph;
